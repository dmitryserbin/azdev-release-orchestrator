trigger: none

pr:
  branches:
    include:
    - master
  paths:
    include:
    - Tasks/*
  autoCancel: true

jobs:
- template: templates/build.yml
  parameters:
    taskV1: true
    taskV2: true
    extension: true
